spring:
  datasource:
    url: jdbc:h2:mem:db;NON_KEYWORDS=USER
    driverClassName: org.h2.Driver
    username: sa
    password: test
  redis:
    host: 127.0.0.1
    port: 6379
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
jwt:
  secret: test
  access-expires-in: 86400
  refresh-secret: testset
  refresh-expires-in: 2592000